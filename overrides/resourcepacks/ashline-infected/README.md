# ashline-infected (resource pack stub)

Minecraft 1.20.1 · pack_format **15**.

## Goal
Replace vanilla green zombie look with ASHLINE infected variants (walker skins, city clothes).

## Layout (TODO art)
```
assets/minecraft/textures/entity/zombie/
  zombie.png          # primary walker — replace at home
  zombie_outer_layer.png
assets/ashline_infected/lang/
  en_us.json
  cs_cz.json
```

Until custom textures exist, paths above are placeholders. Enable this pack in the instance
resource packs list (or ship via Open Loader / global packs when those mods are installed).

Do not recolor to classic Minecraft green Steve.
