# ashline-factions (planned)

Standing runtime is already in **`ashline-senses`** (SavedData + `/ashline standing …`).

This folder is reserved for a later Forge mod that will:
- load `datapacks/ashline/data/ashline/factions/*.json`
- drive NPC trades / contracts / settlement mood (docs/05)

Do not duplicate standing storage here.
